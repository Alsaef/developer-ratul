import Loading from '@/components/UI/Loading/Loading';
import React from 'react';

const loading = () => {
    return (
        <div>
           <Loading></Loading>
        </div>
    );
};

export default loading;